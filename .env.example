# Copy to .env (gitignored) and fill in. Never commit the real token.

# API token of a dedicated test account: Todoist > Settings > Integrations > Developer
TODOIST_TOKEN=

# Environment name from src/config/env.ts (default: prod)
TEST_ENV=prod
